## Example signing configuration (do not commit real secrets)
# Path to your keystore relative to project root
storeFile=keystore/release.jks

# Keystore and key credentials
storePassword=CHANGE_ME
keyAlias=release
keyPassword=CHANGE_ME

